## 2024-05-18 - Added focus-visible styles to header menus
**Learning:** Found an interactive element that was missing proper focus states. Keyboard accessibility is important for users navigating with `Tab`.
**Action:** Always ensure interactive elements (like `Link` or custom `role="button"`) have clear `focus-visible` styles so users can see where they are on the page.
